class RecipeHeader {
  String name;
  String imageUrl;
  int id;

  RecipeHeader({required this.name, required this.imageUrl, required this.id});
}

var e = RecipeHeader(name: "Event 1", imageUrl: "London", id: 0);
